---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: namespaceclasses.qiujian16.github.com.qiujian16.github.com
spec:
  group: qiujian16.github.com.qiujian16.github.com
  names:
    kind: NamespaceClass
    listKind: NamespaceClassList
    plural: namespaceclasses
    singular: namespaceclass
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: NamespaceClass is the Schema for the namespaceclasses API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NamespaceClass
            properties:
              policies:
                description: policies is the policy manifests to be deployed together
                  with the namespaces
                properties:
                  manifests:
                    description: manifests represents a list of kubernetes resources
                      to be applied.
                    items:
                      description: Manifest represents a resource to be applied.
                      type: object
                      x-kubernetes-embedded-resource: true
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
